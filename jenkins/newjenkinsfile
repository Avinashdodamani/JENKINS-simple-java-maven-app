pipeline {
    agent {
        label 'DevNode'
    }

    tools {
        maven 'Maven Jobs'
    }

    stages {
        stage('Build') {
            steps {
                sh "mvn clean package"
            }
        }
    }

    post {
        success {
            archiveArtifacts artifacts: '**/target/*.war'
        }
    }
}
